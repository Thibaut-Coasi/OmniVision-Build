name: build
on: push
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: safwan-abdu/buildozer-action@v1
        with:
          command: android debug
