name: Build
on: push
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: Panda-And-Wolf/buildozer-action@v1
        with:
          command: android debug
