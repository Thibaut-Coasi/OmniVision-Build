name: Build
on: push
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build with Buildozer
        uses: Niklas-dev/buildozer-action@v1
        with:
          command: android debug

