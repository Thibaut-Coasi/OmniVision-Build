name: Build
on: push
jobs:
    main:
        runs-on: unbuntu-latest
        steps:
             -uses: action/checkout@v2
             -uses: ArtemeSerebriakov/buildozer-Action@v1
                  with:
                      command: android debug       
